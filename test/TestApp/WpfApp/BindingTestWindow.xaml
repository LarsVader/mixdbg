<Window x:Class="WpfApp.BindingTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WpfApp.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:CalculatorViewModel}"
        Title="Binding Test - Calculator" Height="400" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="MVVM Binding Test"
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="1" Text="Number A:" VerticalAlignment="Center" Margin="0,0,10,5"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding InputA, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"/>

        <TextBlock Grid.Row="2" Text="Number B:" VerticalAlignment="Center" Margin="0,0,10,5"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding InputB, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,10,0,5">
            <Button Content="Add" Width="80" Margin="0,0,10,0" Command="{Binding AddCommand}"/>
            <Button Content="Multiply" Width="80" Margin="0,0,10,0" Command="{Binding MultiplyCommand}"/>
            <Button Content="Fibonacci" Width="80" Command="{Binding FibonacciCommand}"/>
        </StackPanel>

        <TextBlock Grid.Row="4" Grid.ColumnSpan="2"
                   Text="{Binding Result}" FontSize="18" Foreground="DarkBlue"
                   TextWrapping="Wrap" Margin="0,10,0,5"/>

        <TextBlock Grid.Row="5" Grid.ColumnSpan="2"
                   Text="{Binding StatusMessage}" Foreground="Gray"
                   VerticalAlignment="Bottom"/>
    </Grid>
</Window>
